# wordle
edward ✅ wordle ✅ gay ✅
 in golang

## Dependencies
This project uses Go modules. Commit both `go.mod` and `go.sum` so dependency versions and checksums are reproducible.

Common commands:
- `go mod tidy` to clean up module entries
- `go mod download` to fetch deps
- `go run .` to run code